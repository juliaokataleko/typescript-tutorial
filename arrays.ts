// Arrays

let list1: number[] = [1,2,3];
let list2: Array<number> = [1,2,3]

// Tuples
let person1: [string, number] = ["Julien", 26];

// Emun
enum Color {Red = 5, Green, Blue};

let c: Color = Color.Green;

console.log(c);

